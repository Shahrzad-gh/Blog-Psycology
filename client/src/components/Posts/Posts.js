import React from "react";
import Post from "./Post";

function Posts() {
  // console.log(props);
  return (
    <div className="posts">
      {/* {post.map((p) => (
        <Post data={p} />
      ))} */}
    </div>
  );
}

export default Posts;
